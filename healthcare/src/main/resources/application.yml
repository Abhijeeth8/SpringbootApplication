spring:
  application:
    name: healthcare
  security:
    user:
      name: Abhi
      password: Abhi
  datasource:
    url: jdbc:postgresql://localhost:5432/HealthCareSystem
    username: postgres
    password: Mr.Abhi2
    driver-class-name: org.postgresql.Driver
    hikari:
      auto-commit: true
  jpa:
    #    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      dialect: org.hibernate.dialect.PostgreSQLDialect
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8082
